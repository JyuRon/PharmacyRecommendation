def setupSpec(){} : 테스트 메소드가 여러개 존재하는 경우 첫번째 메소드가 실행될때만 실행한다.
def cleanupSpec(){} : 테스트 메소드가 여러개 존재하는 경우 마지막 메소드가 종료된 이후에 실행한다.
def setup(){} : 모든 테스트 메소드가 실행되기 이전에 실행된다.
def cleanup(){} : 모든 테스트 메소드가 실행된 이후 실행된다.
